# Domain Configuration
DOMAIN=your-domain.com

# Subdomain Security Configuration (Optional)
# Comma-separated list of allowed subdomains for auth cookie access
# If not set, all subdomains are allowed (with warnings logged)
# Use '*' to explicitly allow all subdomains without warnings
# Use '@' or empty string in list to allow main domain
# Examples:
#   ALLOWED_SUBDOMAINS=api,app,admin,@  (allows api.domain.com, app.domain.com, admin.domain.com, and domain.com)
#   ALLOWED_SUBDOMAINS=*  (allows all subdomains without warnings)
#   Leave unset for development (allows all with warnings)
# ALLOWED_SUBDOMAINS=api,app,admin

# SSL/TLS Configuration
LETSENCRYPT_EMAIL=your-email@example.com

# Traefik Configuration
TRAEFIK_DASHBOARD_PORT=8080

# Port Configuration
HTTP_PORT=80
HTTPS_PORT=443

# Auth Middleware Configuration
# Generate with: openssl rand -hex 32
# ⚠️  CRITICAL WARNING ⚠️
# NEVER change COOKIE_SECRET after users have logged in!
# Changing this will invalidate ALL existing sessions/cookies.
# Users will be logged out and need to authenticate again.
# This secret must remain constant across all restarts and deployments.
COOKIE_SECRET=your-secure-random-secret-change-this

# Email Configuration for OTP emails (using Resend)
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here
RESEND_FROM=noreply@yourdomain.com

# MongoDB Configuration
# Root username and password for MongoDB
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change-this-secure-password
# Port for MongoDB (local development only)
MONGO_PORT=27017

# Shorty Stack Configuration
# MongoDB for Shorty application
SHORTY_MONGO_ROOT_USERNAME=shorty_admin
SHORTY_MONGO_ROOT_PASSWORD=change-this-secure-password
SHORTY_MONGO_PORT=27018

# Shorty Application URLs
# Frontend URL for CORS configuration
# Accessible at: https://shorty.your-domain.com
# API accessible at: https://api.shorty.your-domain.com
SHORTY_FRONTEND_URL=https://shorty.your-domain.com

# Docker Images (Production - will use Docker Hub images)
# Leave these unset to use defaults, or override with your registry
# SHORTY_BACKEND_IMAGE=your-registry/shorty-backend:latest
# SHORTY_FRONTEND_IMAGE=your-registry/shorty-frontend:latest
